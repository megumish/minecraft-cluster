apiVersion: v2
name: iam
description: iam applications
type: application
# The following was generated by helm create. So far, I haven't used version and appversion differently.
version: 0.1.0
appVersion: "0.1.0"
